import Puraguin from "../";
import Plex from "../test";

test('test tracker', () => {
    const text = "text";
    expect(text).toEqual("text");
})

test('test class', () => {
    console.log(Plex.prototype);
    expect(true);
})

export default {

} as Puraguin;